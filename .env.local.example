# ─── Existing ───────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
RESEND_API_KEY=re_...
ADMIN_USER=admin
ADMIN_PASS=change-me

# ─── Supabase ───────────────────────────────────────────────────────
# Project ref: srplzxewceuamubcbnzc (Alter5-SW_Architect_Hiring, Frankfurt)
SUPABASE_URL=https://srplzxewceuamubcbnzc.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...           # public, safe in browser (RLS protects)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...   # server-only, bypasses RLS — NEVER expose

# ─── Session signing ────────────────────────────────────────────────
# openssl rand -hex 32
SESSION_SECRET=replace-with-32-bytes-hex

# ─── Cloudflare Turnstile ───────────────────────────────────────────
# Create at https://dash.cloudflare.com/?to=/:account/turnstile
TURNSTILE_SITE_KEY=0x4AAAAA...
TURNSTILE_SECRET_KEY=0x4AAAAA...

# ─── Google Meet booking (Salva) ────────────────────────────────────
# Public Google Calendar Appointment Schedule URL (30-min slot with Salva)
INTERVIEW_BOOKING_URL=https://calendar.app.google/xxxxxx

# ─── Public URLs ────────────────────────────────────────────────────
INTERVIEW_BASE_URL=https://careers.alter-5.com
APPLY_BASE_URL=https://careers.alter-5.com/sw-architect

# ─── Cron (GDPR retention) ──────────────────────────────────────────
# Shared secret for /api/cron/purge-expired. Vercel Cron adds
# `Authorization: Bearer $CRON_SECRET` automatically when defined in
# vercel.json crons and set as a project env var.
# openssl rand -hex 32
CRON_SECRET=replace-with-32-bytes-hex
